function regexVar() {
    
    let re = new RegExp(/\b(a|e|i|o|u)[a-z]{2,}\1\b/);
    
    return re;  
}

function main() {
    const re = regexVar();
    const s = readLine();
    
    console.log(re.test(s));
}
